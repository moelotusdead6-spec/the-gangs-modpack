# GitHub normalizes CRLF to LF on commit unless told otherwise, which breaks
# the file hashes packwiz refresh computes locally. Force LF for served files.
*.toml text eol=lf
*.txt text eol=lf
*.md text eol=lf
